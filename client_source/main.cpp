#include <iostream>
#include <string>
#include <format>
#include <vector>

int main()
{
	
	std::vector<double> vec_x(10);
	std::vector<double> vec_y(10);
	//system("server.exe plot(x,y)"+" x:"to_str(vec_x) +" y:"+to_str(vec_y))
	return 0;
}